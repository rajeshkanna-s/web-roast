import {useEffect,useState} from 'react';
export const slug=page=>page.toLowerCase().replaceAll(' ','-');
export function usePage(pages){const read=()=>{const key=decodeURIComponent(location.hash.replace(/^#\/?/,''));return pages.find(p=>slug(p)===key)||'Home'};const[page,setPage]=useState(read);useEffect(()=>{const change=()=>{setPage(read());window.scrollTo({top:0,behavior:'instant'})};window.addEventListener('hashchange',change);return()=>window.removeEventListener('hashchange',change)},[]);const go=p=>{setPage(p);location.hash='/'+slug(p);window.scrollTo({top:0,behavior:'instant'})};useEffect(()=>{const brand=document.title.split(' — ')[0];document.title=brand+' — '+page},[page]);return[page,go]}
export function useMotion(page){useEffect(()=>{const reduced=window.matchMedia('(prefers-reduced-motion: reduce)');const items=document.querySelectorAll('main section,main article');const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');observer.unobserve(e.target)}}),{threshold:.08});items.forEach(el=>{el.classList.add('motion-reveal');observer.observe(el)});const tilt=e=>{if(reduced.matches||e.pointerType==='touch')return;const el=e.target.closest('.hero-art,.editorial-image,.watch-visual');if(!el)return;const r=el.getBoundingClientRect();el.style.setProperty('--tilt-x',((e.clientY-r.top)/r.height-.5)*-5+'deg');el.style.setProperty('--tilt-y',((e.clientX-r.left)/r.width-.5)*7+'deg')};const reset=e=>{const el=e.target.closest('.hero-art,.editorial-image,.watch-visual');if(el){el.style.setProperty('--tilt-x','0deg');el.style.setProperty('--tilt-y','0deg')}};document.addEventListener('pointermove',tilt);document.addEventListener('pointerout',reset);return()=>{observer.disconnect();document.removeEventListener('pointermove',tilt);document.removeEventListener('pointerout',reset)}},[page])}
